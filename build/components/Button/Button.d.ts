/// <reference types="react" />
import type { TButtonProps } from './Button.types';
declare const Button: ({ label, children, h, onClick, size, styles, variant, w, }: TButtonProps) => JSX.Element;
export default Button;
